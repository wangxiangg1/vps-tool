# vps-tool 轻量 VPS 集中管理面板 PRD

> 文档版本：v0.2 Draft  
> 更新日期：2026-08-23  
> 当前范围：MVP 需求定义，不包含实现代码  
> 部署目标：1 台 1GB RAM 主控服务器 + 多台约 198MB RAM 的 Linux LXC/VPS

## 1. 产品概述

### 1.1 产品定位

vps-tool 是一个面向个人管理员的轻量 VPS 集中管理面板。

主控服务部署在资源相对充足的 Oracle AMD 1GB VPS 上；每台被控 VPS/LXC 仅安装一个轻量 Agent。系统通过 Agent 主动建立的加密长连接完成状态上报、固定动作执行、定时任务和批量操作，不依赖主控持续进行 SSH 轮询。

产品核心目标是：

> 在不明显增加低配置 VPS 资源负担的前提下，集中完成在线监控、WARP 控制与换 IP、3x-ui 状态管理、定时任务和批量操作。

### 1.2 当前使用环境

被控节点主要具备以下特征：

- Linux LXC 或 VPS。
- 内存约 198MB，CPU 和磁盘资源有限。
- 已安装 WARP，并已有可用的启停脚本或命令。
- 已安装 3x-ui 或兼容服务。
- 能够主动访问主控的 HTTPS/WSS 地址。
- 第一版默认使用 systemd；其他 init 系统不在 MVP 范围内。

主控节点具备以下特征：

- Oracle Cloud AMD VPS。
- 1 CPU、1GB RAM、20GB 或以上磁盘。
- 可通过域名提供 HTTPS/WSS 服务。
- 单机、单实例运行，不要求高可用集群。

### 1.3 目标用户

第一版仅服务单一管理员，不提供多租户、团队权限或公开注册。

管理员需要：

- 快速查看所有节点是否在线及其关键状态。
- 在不登录每台 VPS 的情况下操作 WARP 和 3x-ui。
- 创建定时任务并查看可靠的执行结果。
- 对多个节点执行受控的批量操作。
- 在操作失败时快速判断失败阶段和节点当前状态。

## 2. 产品目标与非目标

### 2.1 MVP 目标

1. 节点可以使用一次性注册凭据安装 Agent 并自动上线。
2. 主控可以展示节点在线状态、基础资源、WARP 状态和 3x-ui 状态。
3. 主控可以下发固定动作，并获得可追踪、可去重的执行结果。
4. WARP 换 IP 在控制连接中断时仍可由 Agent 本地完成，并保证最终尝试恢复 WARP。
5. 支持每日或指定 Cron 表达式的定时任务，并避免主控重启后重复执行。
6. 支持有限并发的批量操作，并保证同一节点上的动作串行执行。
7. Agent 在约 198MB RAM 的节点上长期稳定运行，不影响原有代理服务。
8. 系统不提供任何远程任意 Shell 能力。

### 2.2 MVP 不做

- SSH Web 终端。
- 任意 Shell、任意脚本上传或执行。
- 文件管理器和在线配置编辑。
- Docker、Kubernetes、容器编排管理。
- PostgreSQL、Redis、Prometheus、Grafana。
- VPS 系统更新、软件包更新或防火墙管理。
- 多管理员、角色权限、多租户。
- 多主控集群和自动故障转移。
- Agent 自动升级。
- Telegram、邮件等外部通知。
- 非 systemd 系统适配。

## 3. 核心设计原则

### 3.1 被控端轻量化

Agent 只承担以下职责：

- 主动连接主控并维持 WSS 会话。
- 发送心跳和轻量状态数据。
- 接收并执行内置的固定 Action。
- 上报任务状态和执行结果。
- 在本地记录少量命令去重状态。
- 在网络或主控不可用时自动重连。

Agent 不承担以下职责：

- Web 服务和管理界面。
- 数据库和长期日志存储。
- 定时任务调度。
- 复杂时序监控和指标历史聚合。
- 任意命令解释和执行。

### 3.2 主控集中管理

主控负责：

- 管理员认证和 Web 界面。
- 节点、凭据和配置管理。
- Agent 连接管理。
- 命令下发、超时控制和状态汇总。
- 定时任务和批量操作调度。
- 操作日志、安全审计和 SQLite 数据存储。

### 3.3 安全默认拒绝

- Agent 只识别协议中预定义的 Action。
- 未知 Action、未知参数和越界参数一律拒绝。
- 主控不能向 Agent传入命令文本、脚本路径或 Shell 片段。
- 高权限动作通过固定、root 所有且不可由 Agent 用户修改的本地 Helper 完成。
- 节点长期凭据不得出现在安装 URL、查询参数或反向代理访问日志中。

### 3.4 操作可追踪

每次手动、批量或定时操作必须具有唯一请求 ID，并记录完整状态流转。界面不能只显示“请求已发送”，必须区分已排队、已接收、执行中、成功、失败、超时和结果未知。

## 4. 总体架构

```text
                              管理员浏览器
                                    |
                               HTTPS / WSS
                                    |
                    +---------------v----------------+
                    |          Oracle 主控           |
                    |                                 |
                    |  Web UI / API / 登录            |
                    |  Agent WebSocket Gateway        |
                    |  Action Dispatcher              |
                    |  Scheduler / Batch Controller   |
                    |  SQLite / Audit Log             |
                    +---------------+-----------------+
                                    |
                         Agent 主动建立 WSS 连接
                                    |
              +---------------------+---------------------+
              |                     |                     |
        +-----v------+        +-----v------+        +-----v------+
        | VPS/LXC 01 |        | VPS/LXC 02 |        | VPS/LXC 03 |
        | Go Agent   |        | Go Agent   |        | Go Agent   |
        | Root Helper|        | Root Helper|        | Root Helper|
        | WARP/x-ui  |        | WARP/x-ui  |        | WARP/x-ui  |
        +------------+        +------------+        +------------+
```

### 4.1 主控组件

| 组件 | 职责 |
| --- | --- |
| Web UI | 节点列表、详情、任务、日志和设置 |
| API | 管理员操作、节点配置和数据查询 |
| Agent Gateway | Agent 鉴权、连接维护和协议消息处理 |
| Action Dispatcher | 命令下发、去重、超时、结果汇总 |
| Scheduler | 定时任务计算和触发 |
| Batch Controller | 批量操作拆分和并发限制 |
| SQLite | 节点、任务、请求、结果和审计数据持久化 |

### 4.2 Agent 组件

| 组件 | 职责 |
| --- | --- |
| Connection Loop | WSS 连接、心跳、断线重连 |
| Status Collector | 低频采集系统、WARP 和 3x-ui 状态 |
| Action Executor | 校验并执行固定 Action |
| Request Journal | 保存近期请求 ID 和最终结果，避免重复执行 |
| Privileged Helper | 以固定方式完成需要 root 权限的 WARP/x-ui 操作 |
| WARP Watchdog | 换 IP 期间独立保证 WARP 最终恢复 |

## 5. 资源与性能目标

### 5.1 Agent 资源基线

在 198MB RAM、单核 Linux LXC 上连续运行 24 小时后：

- 稳态 RSS 目标小于 10MB，MVP 硬性上限为 15MB。
- 空闲 CPU 一分钟平均占用不高于 0.2%。
- 资源占用不得持续增长。
- 单文件二进制，支持 `linux/amd64` 和 `linux/arm64`。
- 不内置数据库；仅允许写入体积受限的配置和请求去重状态文件。
- 主控失联不影响 WARP、3x-ui 和节点原有业务继续运行。

资源验收以实际 RSS 和持续运行数据为准，不以编译后二进制大小代替内存指标。

### 5.2 主控资源基线

在 1 CPU、1GB RAM 主机上，以 100 个在线 Agent 为基准：

- 主控应用稳态 RSS 目标不高于 350MB。
- 100 条空闲 WSS 连接可持续运行。
- 节点列表和日志常规查询在 2 秒内返回。
- 批量任务默认最大并发为 5，可由管理员在安全范围内调整。
- SQLite 数据库增长、备份和日志保留可控。

## 6. 管理员登录

### 6.1 登录方式

MVP 使用用户名和密码登录，仅允许创建一个管理员账号。

安全要求：

- 密码使用 Argon2id 或 bcrypt 哈希保存。
- 登录状态使用服务端 Session 和 Cookie。
- Cookie 必须设置 `HttpOnly`、`Secure` 和适当的 `SameSite`。
- 所有改变状态的 Web 请求必须具备 CSRF 防护。
- 登录接口需要限速和连续失败保护。
- 日志中不得记录密码、Session、注册 Token 或 Agent 凭据。

### 6.2 后续能力

- TOTP 两步验证。
- 管理端 IP 白名单。
- 多管理员和操作权限。

以上不属于 MVP。

## 7. 节点注册与生命周期

### 7.1 创建节点

管理员在面板中创建节点，填写：

- 节点名称。
- 地区或标签。
- WARP 适配器类型。
- 3x-ui systemd 服务名。
- 可选备注。

主控生成：

- 不可变的内部 `node_id`。
- 有效期默认 10 分钟、仅可使用一次的注册 Token。
- 对应 CPU 架构的安装命令。

节点名称允许修改，`node_id` 不允许修改或复用。

### 7.2 安装流程

```text
下载公开安装器
    |
识别 amd64/arm64 和 systemd
    |
下载对应 Agent 二进制
    |
校验 SHA-256 或发布签名
    |
写入权限为 0600 的初始配置
    |
使用一次性 Token 向主控注册
    |
换取节点长期凭据
    |
注册 Token 立即失效
    |
安装并启动 systemd 服务
    |
Agent 通过 WSS 上线
```

长期 Agent 凭据不得嵌入下载 URL。一次性 Token 即使出现在管理员 Shell 历史中，也必须因短有效期和单次使用而快速失效。

### 7.3 长期凭据

- 每个节点使用独立随机凭据。
- 服务端只保存可验证哈希，不保存明文凭据。
- Agent 本地配置权限必须为 `0600`。
- 支持在面板中吊销和轮换凭据。
- 一个节点同一时刻只允许一个有效主连接；新旧连接冲突时记录安全日志。
- `node_id` 只用于标识，不作为认证凭据。

### 7.4 删除节点

删除节点时必须二次确认。删除后：

- 立即吊销 Agent 凭据并断开连接。
- 停止该节点后续定时任务。
- 保留历史操作日志，节点显示为已删除。
- 不自动卸载远端 Agent，MVP 仅提供明确的手动卸载说明。

## 8. Agent 连接与状态上报

### 8.1 连接方式

- Agent 主动连接 `wss://panel.example.com/agent`。
- 生产环境禁止明文 WebSocket。
- 主控无需开放被控节点的入站管理端口。
- Agent 不依赖主控主动 SSH 探测。

### 8.2 心跳和离线判定

- Agent 每 30 秒发送一次应用层心跳。
- 主控连续 90 秒未收到心跳后标记为 `Offline`。
- 主控以消息接收时间作为 `last_seen_at`，不完全信任节点本地时钟。
- Agent 连接成功后立即发送一次完整状态。
- 心跳只携带轻量字段；完整资源状态默认每 60 秒上报一次。
- 公网 IP 默认每 5 分钟、首次上线及 WARP 操作后检测，避免频繁请求外部服务。

### 8.3 自动重连

Agent 使用指数退避并加入随机抖动：

```text
1s -> 2s -> 4s -> 8s -> 16s -> 30s -> 最大 60s
```

连接成功后重置退避。断线重连不得高频写日志或产生明显 CPU 占用。

### 8.4 上报字段

- Node ID。
- Agent 版本和协议版本。
- 主机名、系统名称、系统版本和 CPU 架构。
- CPU 使用率。
- 内存已用量、总量和使用率。
- 根分区已用量、总量和使用率。
- 系统 Uptime。
- WARP 状态。
- 3x-ui 状态。
- WARP 出口 IPv4 和可选 IPv6。
- 状态采集时间。

公网 IP 在产品中统一表示“当前业务出口 IP”。如需要展示未经过 WARP 的直连 IP，应使用单独字段，禁止混用。

## 9. Agent 通信协议

### 9.1 消息信封

所有消息至少包含：

```text
protocol_version
message_type
message_id
sent_at
payload
```

主控与 Agent 必须拒绝不兼容的协议版本，并返回明确错误，不得尝试按未知结构执行动作。

### 9.2 消息类型

- `agent_hello`
- `heartbeat`
- `status_report`
- `command`
- `command_ack`
- `command_progress`
- `command_result`
- `server_notice`

### 9.3 命令字段

主控下发命令至少包含：

```text
request_id
node_id
action
issued_at
deadline_at
parameters
```

`parameters` 只能包含该固定 Action 明确定义的结构化参数，不能包含命令字符串、可执行文件路径或脚本正文。

### 9.4 状态流转

```text
queued
  -> dispatched
  -> accepted
  -> running
  -> succeeded | failed | timed_out | unknown
```

附加终态：

- `expired`：未下发前已超过截止时间。
- `skipped_offline`：定时任务执行时节点离线且不允许补跑。
- `canceled`：仅允许取消尚未开始执行的请求。

主控在连接中断后不能把“没有收到结果”直接显示成失败，应标记为 `unknown`，等待 Agent 重连后同步最终状态。

### 9.5 去重和并发

- `request_id` 全局唯一。
- 主控对同一请求最多按至少一次语义投递，Agent 必须去重。
- Agent 在本地保存近期请求 ID、Action 和终态结果，重启后仍能识别重复请求。
- 去重记录数量和文件体积必须设置上限。
- 同一节点同一时刻只执行一个改变状态的 Action。
- 状态采集可与动作并行，但不得干扰动作执行。
- 重复请求必须返回之前的状态或结果，不能再次执行。

### 9.6 离线行为

- 节点离线时，手动操作按钮不可执行，并提示节点最后在线时间。
- MVP 不对手动操作进行无限离线排队。
- 定时任务在节点离线时记为 `skipped_offline`，按任务重试策略处理。
- 节点恢复后不执行已经超过补跑窗口的旧任务。

## 10. 固定 Action

MVP 只允许：

| Action | 作用 | 是否改变状态 |
| --- | --- | --- |
| `get_status` | 立即采集节点状态 | 否 |
| `get_ip` | 检测当前出口 IP | 否 |
| `warp_on` | 开启 WARP | 是 |
| `warp_off` | 关闭 WARP | 是 |
| `change_ip` | 本地完成完整换 IP 流程 | 是 |
| `restart_xui` | 重启配置的 3x-ui 服务 | 是 |

未知 Action 必须返回 `unsupported_action`，不得回退为 Shell 执行。

## 11. WARP 管理

### 11.1 WARP 状态

统一状态为：

- `on`
- `off`
- `degraded`
- `unknown`

`degraded` 表示服务或隧道看似运行，但出口检测不符合预期。状态判断应由节点安装时选定的 WARP 适配器完成。

### 11.2 WARP 适配器

不同节点可能使用不同 WARP 实现和现有脚本。MVP 允许在安装或节点配置阶段选择受支持的适配器，但每个适配器只能映射到 Agent 包内或 root 所有的固定 Helper：

- `status`
- `on`
- `off`

主控不得上传脚本，也不得指定任意脚本路径。Helper 文件必须由 root 所有，Agent 运行用户无写权限。

### 11.3 开启和关闭

`warp_on`：

1. 检查当前状态。
2. 已开启时直接返回成功和 `already_on`。
3. 调用固定 Helper 开启 WARP。
4. 在限定时间内检查服务和出口状态。
5. 返回最终状态和耗时。

`warp_off`：

1. 检查当前状态。
2. 已关闭时直接返回成功和 `already_off`。
3. 调用固定 Helper 关闭 WARP。
4. 验证 WARP 已关闭。
5. 如果控制连接经过 WARP，允许 WSS 暂时中断；结果在重连后补报。

### 11.4 换 IP 本地状态机

`change_ip` 必须作为一个完整 Action 一次下发。Agent 接收后独立完成全部步骤，不依赖主控在 WARP 关闭后继续发送命令。

```text
预检 WARP 状态和本地 Helper
    |
获取旧出口 IP
    |
启动独立恢复 Watchdog
    |
关闭 WARP
    |
等待隧道退出
    |
开启 WARP
    |
等待网络、DNS 和隧道恢复
    |
获取新出口 IP
    |
比较新旧 IP
    |
相同则在上限内重试
    |
确认 WARP 最终为 ON
    |
取消 Watchdog并保存结果
```

默认策略：

- 仅当 WARP 当前为 `on` 或 `degraded` 时允许换 IP；`off` 时返回 `invalid_state`。
- 最大尝试次数为 3 次。
- 单次 Action 总超时默认 180 秒。
- 每次启停和出口检测使用独立的子超时。
- 如果控制连接中断，状态机继续本地执行。
- 结果持久化到请求去重记录，重连后补报。

### 11.5 WARP 恢复 Watchdog

Watchdog 必须独立于当前 WSS 会话，并尽量独立于 Agent 主进程。其职责是：

- 在换 IP 开始时设置有限期恢复任务。
- 如果 Agent 崩溃、连接中断或状态机异常退出，仍尝试重新开启 WARP。
- 确认状态机正常完成且 WARP 已开启后再撤销恢复任务。
- 恢复任务必须有最大尝试次数，避免无限循环消耗资源。

如果环境允许，Agent 到主控的控制流量应绕过 WARP；即使无法绕过，Watchdog 和本地状态机仍必须保证流程可完成。

### 11.6 换 IP 结果

成功结果包含：

- 旧出口 IP。
- 新出口 IP。
- 尝试次数。
- 最终 WARP 状态。
- 总耗时。

失败需要区分：

- `precheck_failed`
- `old_ip_unavailable`
- `warp_stop_failed`
- `warp_start_failed`
- `network_not_recovered`
- `ip_check_failed`
- `ip_unchanged`
- `action_timeout`
- `result_unknown`

即使新旧 IP 最终相同，也必须单独报告最终 WARP 状态，不能仅显示“失败”。

## 12. 3x-ui 管理

### 12.1 服务配置

默认支持以下 systemd 服务名：

- `x-ui`
- `3x-ui`
- `xray-ui`

允许安装时填写自定义 systemd Unit 名称，但必须：

- 通过严格的 Unit 名称格式校验。
- 只作为 `systemctl` 的一个 Unit 参数使用。
- 不允许空格、Shell 元字符、路径或额外参数。
- 保存后在面板中清晰展示。

### 12.2 状态

统一状态为：

- `running`
- `stopped`
- `failed`
- `not_found`
- `unknown`

### 12.3 重启流程

1. 通过固定 Helper 查询 Unit 是否存在。
2. 记录重启前状态。
3. 执行受限的 restart 动作。
4. 等待服务状态稳定。
5. 使用 `systemctl is-active` 等只读检查验证结果。
6. 返回前后状态、耗时和受限长度的错误摘要。

服务命令输出需要限制长度并清理敏感数据，不能无限写入数据库。

## 13. 节点列表

首页默认展示：

- 节点名称。
- Node ID 缩写。
- 地区或标签。
- 在线状态。
- 当前出口 IPv4。
- WARP 状态。
- 3x-ui 状态。
- CPU 使用率。
- 内存已用量和总量。
- Uptime。
- Agent 版本。
- 最后在线时间。
- 最后换 IP 时间和结果。

支持：

- 按在线状态、地区、WARP 状态和 3x-ui 状态筛选。
- 按名称和 Node ID 搜索。
- 选择多个节点执行批量操作。
- 对状态过期的数据明确显示“数据更新时间”，不能伪装成实时值。

## 14. 节点详情

详情页展示：

- 节点基本信息和标签。
- 系统、架构、内核和 Agent 版本。
- 在线状态、连接时间和最后心跳。
- CPU、内存、根分区和 Uptime。
- 当前出口 IPv4/IPv6。
- WARP 和 3x-ui 状态。
- 最近操作和最近失败原因。
- 关联的定时任务。

可执行操作：

- 立即刷新状态。
- 获取出口 IP。
- 开启 WARP。
- 关闭 WARP。
- 换 IP。
- 重启 3x-ui。
- 轮换或吊销 Agent 凭据。

关闭 WARP、重启 3x-ui 和删除节点需要二次确认。执行中按钮必须禁用，并显示请求当前状态。

## 15. 定时任务

### 15.1 支持的 Action

- `change_ip`
- `warp_on`
- `warp_off`
- `restart_xui`

### 15.2 任务字段

- 任务名称。
- 单个节点或一组节点。
- 固定 Action。
- 调度规则。
- IANA 时区。
- 是否启用。
- 最大重试次数。
- 重试间隔。
- 补跑窗口。
- 创建和更新时间。

### 15.3 调度规则

MVP 至少支持：

- 每天指定时间。
- 每周指定星期和时间。
- 受校验的 Cron 表达式。

默认使用主控配置的 `Asia/Shanghai` 时区，每个任务可以选择其他 IANA 时区。界面必须显示任务时区，禁止仅保存无时区的 `04:00`。

### 15.4 重试和补跑

- 默认失败重试 2 次。
- 默认重试间隔为 30 秒和 90 秒。
- 每次实际执行生成独立 `request_id`，同一次调度触发使用同一个 `task_run_id`。
- 主控短暂重启后，只补跑仍在补跑窗口内的任务。
- 默认补跑窗口为 5 分钟。
- 超出窗口记录为 `missed`，节点恢复后不再补执行。
- `ip_unchanged` 可以重试；认证失败、配置错误等不可恢复错误不应无意义重试。

### 15.5 单调度器约束

MVP 必须保证同一数据库只有一个有效 Scheduler。FastAPI/Uvicorn 不得通过多个 worker 各自启动 APScheduler，否则可能重复触发任务。

如未来扩展多实例，必须先实现明确的调度器选主或数据库锁，不属于 MVP。

## 16. 批量操作

支持选择多个在线节点执行：

- 批量换 IP。
- 批量开启 WARP。
- 批量关闭 WARP。
- 批量重启 3x-ui。

执行规则：

- 默认全局最大并发为 5。
- 同一节点始终只执行一个改变状态的 Action。
- 每个节点生成独立 `request_id`。
- 批量操作具有独立 `batch_id`，用于汇总进度。
- 单节点失败不阻断其他节点。
- 界面实时显示总数、等待、执行中、成功、失败和未知数量。
- 执行前显示节点清单和 Action，并要求确认。
- 离线节点默认跳过并单独列出。

## 17. 日志与审计

### 17.1 操作日志

每次 Action 至少记录：

- Request ID。
- 可选 Batch ID、Task ID 和 Task Run ID。
- 节点 ID 和当时的节点名称。
- 操作来源：手动、批量或定时。
- Action。
- 创建、下发、接收、开始和结束时间。
- 状态和错误码。
- 受限长度的错误摘要。
- 执行耗时和尝试次数。
- WARP 换 IP 的旧 IP、新 IP 和最终状态。

### 17.2 安全审计

记录：

- 管理员登录成功和失败。
- 节点创建、删除、配置修改。
- 注册 Token 创建、使用、过期。
- Agent 凭据轮换和吊销。
- 重复连接和认证失败。
- 定时任务创建、修改、启停和删除。

不得记录任何明文凭据或完整 Session。

### 17.3 保留策略

- 操作和审计日志默认保存 90 天。
- 保留天数可在 7 至 365 天范围内配置。
- 定期分批清理，避免长事务阻塞 SQLite。
- 关键统计可以保留聚合结果，但 MVP 不做长期监控曲线。

## 18. 数据模型

MVP 建议包含：

| 表 | 主要用途 |
| --- | --- |
| `users` | 管理员账号和密码哈希 |
| `sessions` | 管理端登录会话 |
| `nodes` | 节点信息、配置和最后状态 |
| `agent_credentials` | 节点凭据哈希、状态和轮换信息 |
| `enrollment_tokens` | 一次性注册 Token 哈希和有效期 |
| `action_requests` | 手动和批量命令及其状态 |
| `action_results` | 命令结果和结构化摘要 |
| `scheduled_tasks` | 定时任务定义 |
| `task_runs` | 每次调度触发记录 |
| `batches` | 批量操作汇总 |
| `audit_logs` | 管理和安全审计 |
| `settings` | 主控级设置 |

实时 WSS 连接保存在内存中，不需要为每次心跳写入独立历史表。节点最后状态可以按受控频率更新，避免无意义的高频 SQLite 写入。

SQLite 要求：

- 启用 WAL。
- 启用外键约束。
- 配置合理的 busy timeout。
- 所有 Schema 变更使用版本化迁移。
- 定期进行一致性检查和备份。
- 备份必须使用 SQLite 在线备份机制或经过验证的安全方式，不能在写入期间直接复制不完整文件。

## 19. 安全设计

### 19.1 传输安全

- Web 和 Agent 通信仅允许 HTTPS/WSS。
- TLS 证书必须有效，Agent 默认不得跳过证书校验。
- 反向代理需要正确支持 WebSocket 升级和长连接超时。
- 敏感接口禁止通过 URL 查询参数传递凭据。

### 19.2 权限隔离

- Agent 以专用低权限系统用户运行。
- 需要 root 的动作通过固定 Helper 和严格 sudoers 白名单执行。
- Helper 由 root 所有，普通用户不可写。
- sudoers 只能允许确定的 Helper，不能允许通用 Shell、解释器、`systemctl *` 或任意参数组合。
- Agent 配置、状态目录和日志目录使用最小权限。
- systemd 服务采用与 Helper 调用兼容的沙箱和文件系统限制。

### 19.3 Action 安全边界

以下能力在任何情况下都不能通过配置偷偷引入：

- 任意 Shell 命令。
- 任意脚本路径。
- 任意 systemd 参数。
- 任意文件读写。
- 主控上传并立即执行二进制。

如未来确需扩展 Action，必须在 Agent 新版本中显式增加、审查并发布，不能由主控动态定义。

### 19.4 发布与安装安全

- Agent 发布物提供 SHA-256 清单或签名。
- 安装器先校验再替换二进制。
- 安装失败不得破坏现有 WARP 和 3x-ui 服务。
- MVP 不提供自动升级；手动升级需保留旧二进制以便回滚。

## 20. 错误处理

所有错误同时具备机器可识别的错误码和面向管理员的中文说明。

通用错误码至少包括：

- `node_offline`
- `request_expired`
- `request_duplicate`
- `action_busy`
- `unsupported_action`
- `invalid_parameters`
- `invalid_state`
- `permission_denied`
- `helper_not_found`
- `helper_failed`
- `action_timeout`
- `result_unknown`
- `agent_version_incompatible`

界面应展示可操作的信息，例如“服务名不存在，请检查节点配置”，而不是只展示内部异常堆栈。

## 21. 技术栈与部署约束

### 21.1 主控

- Python。
- FastAPI。
- Uvicorn 单 worker。
- SQLite。
- APScheduler。
- WebSocket。
- 服务端模板或轻量 HTML/CSS/JavaScript 前端。

MVP 优先使用轻量前端，不要求 React。前后端保持同源部署，降低认证、CSRF 和部署复杂度。

### 21.2 Agent

- Go。
- 单文件静态或近静态二进制。
- 尽量使用标准库和少量经过审查的依赖。
- 构建时去除不必要的调试信息。
- 支持 `linux/amd64` 和 `linux/arm64`。

### 21.3 主控部署

推荐形态：

```text
Nginx/Caddy TLS 反向代理
    |
单实例 FastAPI/Uvicorn
    |
SQLite 数据文件和备份目录
```

主控进程和 Scheduler 的启停必须统一，避免出现两个调度器同时运行。部署升级前必须备份数据库，并验证升级后 WebSocket、Scheduler 和数据库迁移状态。

## 22. 可用性要求

- Agent 断线后自动重连，无需人工干预。
- 主控重启后 Agent 自动恢复连接。
- 主控不可用期间，不影响节点原有 WARP 和 3x-ui 服务。
- 主控恢复后能够继续接收节点状态。
- 正在执行的 Action 如果因断线无法确认，显示为 `unknown`，重连后再对账。
- WARP 换 IP 无论成功或失败，都必须优先保证 WARP 最终恢复为 ON。
- SQLite 备份可以恢复到一台新主控，并保留节点、任务和历史日志。

## 23. MVP 验收标准

### 23.1 节点与注册

- 可以创建、编辑和删除节点。
- 一次性注册 Token 只能使用一次且会过期。
- Agent 安装后自动上线。
- 吊销凭据后 Agent 无法重新连接。
- 重复 Node ID 或重复连接不会覆盖其他节点。

### 23.2 在线状态

- Agent 每 30 秒心跳，超过 90 秒正确显示离线。
- 主控重启后 Agent 能自动重连。
- 列表和详情页能展示数据更新时间。
- 断网期间 Agent 不出现高 CPU 或无限日志增长。

### 23.3 WARP

- 可以查看 ON、OFF、Degraded 和 Unknown 状态。
- 可以开启和关闭 WARP。
- 可以执行一次性 `change_ip` Action。
- 新旧出口 IP 不同才判定换 IP 成功。
- IP 未变化时按策略重试并返回明确错误。
- 关闭 WARP 导致 WSS 断开时，Agent 仍能本地继续开启 WARP。
- 模拟 Agent 崩溃或状态机异常后，Watchdog 会尝试恢复 WARP。
- 每次结果包含最终 WARP 状态。

### 23.4 3x-ui

- 能区分 Running、Stopped、Failed、Not Found 和 Unknown。
- 可以重启受支持的服务。
- 自定义服务名不能注入额外 systemctl 参数或 Shell 内容。
- 重启后通过实际服务状态确认成功，不能只根据命令退出码判断。

### 23.5 任务可靠性

- 命令状态完整可追踪。
- 重复投递同一 `request_id` 不会重复执行。
- 同一节点不会并行执行两个改变状态的 Action。
- Agent 重连后可以补报已执行请求结果。
- 主控重启不会重复触发同一次定时任务。
- 超出补跑窗口的任务不会在节点恢复时突然执行。

### 23.6 定时与批量

- 可以创建、修改、暂停、恢复和删除定时任务。
- 任务按指定 IANA 时区执行。
- 失败任务按照错误类型和策略重试。
- 可以批量执行四类固定 Action。
- 并发不超过配置上限，单节点失败不影响其他节点。
- 批量进度和各节点结果可查看。

### 23.7 安全

- Agent 无任意 Shell 接口。
- 服务器下发未知 Action 时 Agent 拒绝执行。
- 长期 Agent 凭据不出现在 URL 和服务端日志中。
- 服务端不保存明文长期凭据。
- Agent 用户不能修改 root Helper。
- 登录、Session、CSRF 和密码存储符合本 PRD 要求。

### 23.8 资源

- Agent 在目标 LXC 上连续运行 24 小时，RSS 不超过 15MB且无持续增长。
- Agent 空闲 CPU 一分钟平均不超过 0.2%。
- 100 个模拟或真实 Agent 长连接下，主控在 1GB RAM 环境稳定运行。
- 批量操作和日志清理不会使主控失去响应。

## 24. 实施阶段建议

### 阶段 0：技术验证

- 在真实 198MB LXC 上验证 Go Agent 内存和重连行为。
- 验证关闭 WARP 是否会切断 Agent 到主控的路由。
- 验证独立 Watchdog 可以在 Agent 异常时恢复 WARP。
- 清点实际 WARP 脚本类型和 3x-ui Unit 名称。
- 验证 amd64/arm64 发布物和 systemd 环境。

### 阶段 1：连接和状态

- 管理员登录。
- 节点创建和一次性注册。
- Agent WSS、心跳、重连和基础状态。
- 节点列表和详情页。

### 阶段 2：固定 Action

- Action 协议、状态流转和本地去重。
- WARP ON/OFF。
- WARP 换 IP 状态机和 Watchdog。
- 3x-ui 状态和重启。
- 操作日志。

### 阶段 3：调度和批量

- APScheduler 单实例调度。
- 时区、补跑和重试。
- 批量拆分、并发限制和结果汇总。

### 阶段 4：加固和验收

- 凭据轮换和吊销。
- SQLite 备份恢复。
- 资源压力测试。
- 断网、主控重启、Agent 崩溃和重复请求测试。
- 安装包校验和部署文档。

## 25. 第二阶段候选功能

- Telegram 或邮件通知。
- IP 历史和变化统计。
- 自动健康检查和受控自愈。
- Agent 签名升级、灰度发布和回滚。
- TOTP 和管理端 IP 白名单。
- 更多固定 Action。
- 资源历史趋势。
- 多主控或调度器选主。

任何新增 Action 仍必须遵守“Agent 显式实现、固定参数、无任意 Shell”的安全边界。

## 26. 开发前待确认项

以下信息需要在技术设计开始前通过真实节点验证或确认：

1. 当前节点实际使用哪些 WARP 实现和启停脚本。
2. 关闭 WARP 后，Agent 到主控的路由是否会中断。
3. Watchdog 采用 systemd transient timer、固定 Helper 子进程还是其他可恢复机制。
4. 各节点真实的 3x-ui systemd Unit 名称。
5. 节点 CPU 架构分布是否只有 amd64/arm64。
6. 公网出口 IP 检测服务、超时和备用检测源。
7. 首期真实节点数量及未来一年的预估上限。
8. 主控使用现有 Nginx/1Panel 还是独立反向代理部署。

这些待确认项不改变产品方向，但会直接影响 Agent 适配器、网络恢复策略和验收测试环境。

## 27. 最终产品体验

管理员登录后直接进入节点工作台：

```text
节点       出口 IP        WARP       3x-ui       状态

SG-01      1.2.3.4        ON         Running     Online
JP-01      5.6.7.8        ON         Running     Online
US-01      9.8.7.6        OFF        Running     Online
HK-01      2.3.4.5        Unknown    Stopped     Offline

[ 换 IP ]  [ 开启 WARP ]  [ 关闭 WARP ]  [ 重启 3x-ui ]
```

核心体验保持简单：状态一眼可见、操作结果可追踪、失败原因能理解、低配置节点无明显额外负担。

第一版的优先级依次是：

1. 不破坏节点现有网络和服务。
2. 动作可靠且不会重复执行。
3. Agent 足够轻量。
4. 安全边界明确。
5. 操作简单。

